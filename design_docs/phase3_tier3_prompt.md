# Prompt: Phase 3, Tier 3 — config/documentation-shaped, cheap, opportunistic

Paste this to the implementing agent as-is.

---

Execute Phase 3, Tier 3 of `docs/AUTOPILOT_REFACTOR_PLAN.md` — the final Phase 3 batch. Tiers 1 and 2 are done; read `design_docs/phase3_tier2_findings.md` for the established rigor and format before starting, same conventions apply here (regression test first where the fix has real behavior to characterize — several of these items don't — no commits, leave in the working tree for review).

## Read first

`docs/AUTOPILOT_REFACTOR_PLAN.md` §5, Tier 3, items 22-28. The plan's own framing is "cheap, do opportunistically" — this tier is genuinely different in shape from Tiers 1-2: mostly docs/comment fixes, one real dead-code deletion, one real deprecated-API migration with a bigger-than-documented surface, and two items the plan explicitly says were *found but deliberately not fixed* (test-hygiene notes, not bugs to close). Don't force every item into the same "regression test, fix, verify" shape — match the item's actual nature.

## Freshness check — confirmed as of this handoff, re-verify

- **Item 22 (`git_commit_push.yaml`'s contradictory description) — still live, exactly as described.** `config/workflows/autopilot/git_commit_push.yaml`'s `description` field says "The pipeline must not commit, push, create pull requests, or merge branches. An operator reviews the validated worktree and explicitly authorizes any Git action" — directly contradicted by the same file's STEP 1-5 body, which instructs the agent to autonomously stage, commit, push, and merge/create-PR (with `review_mode` only conditionally gating the merge step, not blocking the whole phase). Rewrite the description to match the body's actual (review_mode-gated-merge, not human-only) behavior.
- **Item 23 (7 dead `Workflow.status.in_(["active", "running"])` branches) — still live, but the count is 8, not 7.** `Workflow.status`'s `CheckConstraint` (`src/core/database.py:392`) only permits `('active', 'completed', 'paused', 'failed')` — `"running"` has never been a valid value, confirmed. Fresh grep found 8 sites across 5 files: `src/monitoring/orphan_reaper.py:85`, `src/autopilot/orchestrator/engine_client.py:433`, `src/mcp/autopilot/control_routes.py:107,135,644`, `src/mcp/autopilot/queue_routes.py:156,390`, and `src/autopilot/orchestrator/queue.py:63` (a plain Python tuple membership check, `if wf_status not in ("active", "running", "paused")`, not a DB query filter — same dead value, different mechanism). Re-verify this count yourself; re-grep rather than trusting this list, since several of these files were touched by this session's own Tier 1/2 work and could have shifted again.
- **Item 24 (ticket-creation friction prompt/schema plumbing) — needs a fresh check, not a re-read.** `format_initial_message` now lives in `src/agents/prompt_builder.py` (not wherever it was when this item was written) — a quick grep for `ticket_id`/`ticket_type` in that file this handoff found only one loosely-related match (a query filter, not the described prompt warning text), meaning either the specific text moved elsewhere, was rephrased, or this handoff's grep missed it. Actually locate the `ticket_id` precondition warning and the phase-agent exemption list this item describes before concluding anything about their current state — this handoff did not find and read them, only searched for them.
- **Item 25 (`MonitoringLoop` collaborator compat debt) — still live, exactly as described.** `MAX_STUCK_TASK_NUDGES` is defined in both `src/monitoring/monitor.py:157` and `src/monitoring/health_audit.py:20`; `UNCONFIRMED_COMPLETION_ESCALATE_AFTER` in both `monitor.py:173` and `src/monitoring/mechanical_recovery.py:53`. This is explicitly **not a bug to fix** per the plan's own text — it's the deliberate cost of the delegator-stub compatibility approach, real duplication that collapses once the ~180 tests calling `MonitoringLoop` methods directly get ported to construct the new collaborator classes — a bigger undertaking than this tier's "cheap, opportunistic" framing covers. Treat as documentation/awareness, not a fix target, unless you find it's become trivially closable.
- **Item 26 (stale cross-file comments) — one of three sub-claims still live, the other two are stale or already resolved.** `src/phases/phase_manager.py:1207` (line shifted from the plan's cited 1256) still literally says `"task_completion_service.py's fire_spec_gate_if_ready"` — confirmed wrong, `fire_spec_gate_if_ready` is at `src/autopilot/orchestrator/phase_transitions.py:3288`. The other two named instances no longer match: `phase_manager.py:1477` and `worktree_integration.py:669` both reference `fire_spec_gate_if_ready` by name only, with no file attribution to correct. `queue_service.py`'s docstring (now around line 165-180) cites a code excerpt from `manager.py`, not a line number — the specific "pre-extraction line number" claim the plan describes doesn't appear to exist anymore. Re-verify all of this yourself; fix only what's actually still wrong.
- **Item 27 (two test-hygiene items, found but deliberately not fixed) — both still live.** `tests/manual_validation_test.py` still fires two `requests.post(...)` calls at module level (lines 11, 29) — import-time, not inside a test function, which hangs a bare `pytest tests/` run. `tests/test_task_completion_service.py::test_passes_when_no_workflow_id_and_files_in_feature_dir` (still at that name, line 108) still relies on ambient filesystem state per the plan's description — re-read it yourself to confirm the exact mechanism before fixing. **These were explicitly excluded from Phase 1b's own full-suite gate, not fixed** — the plan lists them as known, deliberately deferred debt. This tier is a reasonable place to actually close them, but confirm that's the intent (they're cheap and clearly-scoped) rather than assuming silently.
- **Item 28 (`libtmux`'s deprecated `attached_window`/`attached_pane`) — still live, and the surface is meaningfully bigger than the plan's partial list.** The plan named 4 files (`manager.py`, `launch_pipeline.py`, `terminator.py`, `output_capture.py`) and explicitly flagged its own list as partial ("grep the rest of `src/` before fixing"). Fresh grep found **8 files, 12 occurrences**: the original 4 (`launch_pipeline.py` alone has 4) plus `src/agents/messenger.py`, `src/services/agent_communication.py`, `src/monitoring/mechanical_recovery.py`, and `src/autopilot/orchestrator/engine_client.py` — none of the last four were in the plan's list, likely surfaced by this session's own decomposition work spreading the pattern into new files. The plan's cited failing-test list (`test_stable_transcript.py`, `test_transcript_processing.py`, `test_agent_manager.py`, `test_orphan_reaper.py`) needs a fresh run — this handoff's own attempt to run all four together hung past a 90s timeout (possibly unrelated, re-verify); each file individually is a saner way to get a current failure count before starting.

## Target

- **Item 22**: rewrite `git_commit_push.yaml`'s `description` to describe the actual, `review_mode`-gated autonomous behavior — don't just soften the "must not" language, make it accurately state what STEP 4 actually does in each mode.
- **Item 23**: delete the dead `"running"` value from each of the (re-verified count of) filter/membership-check sites. This is a pure dead-value removal — `"active"` (and `"paused"` where present) still does the real work; removing `"running"` changes nothing about which rows match, only removes a value that could never have matched anything.
- **Item 24**: locate the actual `ticket_id` precondition warning and phase-agent exemption list in `prompt_builder.py` (or wherever it's actually moved to), and confirm both are still correct and complete — this is a verification item, only touch code if you find something genuinely wrong, not a guaranteed fix.
- **Item 25**: no fix — document current state is unchanged, matching the plan's own "not fixed per the zero-behavior-change rule" framing, unless something about the situation has genuinely changed enough to make this now-cheap (state that explicitly if so, don't just do it because it's tempting).
- **Item 26**: fix the one confirmed-stale comment (`phase_manager.py:1207`) to name the correct current file. Leave the other two sub-claims alone — they're not wrong, just no longer literally as the plan described.
- **Item 27**: for both, decide (and state) whether to actually fix them here or continue deferring — if fixing: move `manual_validation_test.py`'s HTTP calls behind a fixture/test function (not module scope); give `test_passes_when_no_workflow_id_and_files_in_feature_dir` its own isolated fixture instead of relying on ambient `.hephaestus/features/` state.
- **Item 28**: migrate every confirmed `attached_window`/`attached_pane` call site (re-verify the file list yourself, this handoff's 8-file/12-occurrence count is a starting point) to `active_window`/`active_pane`, matching libtmux ≥ 0.31's API. Confirm the previously-failing tests (re-run individually) go green.

## Verification

- Items 22, 26: no meaningful automated test (text/comment content) — read the changed file back and confirm it says what it should.
- Item 23: a quick check that the dead branches are gone and no test regresses (these sites already have coverage from Tier 1/2 work this session in several cases — reuse it, don't invent new tests for a value-removal with no behavioral surface).
- Item 24: whatever you find wrong (if anything) gets a normal regression test; if nothing's wrong, no test needed, just state what you checked.
- Item 27: if fixed, a test proving `manual_validation_test.py` no longer executes HTTP calls at import time (e.g. a static check, or confirming `pytest --collect-only` doesn't trigger network I/O) and a passing isolated run of the flaky fixture test from a clean directory.
- Item 28: the four previously-failing test files (rerun individually, not combined, given this handoff's own hang) must go green after the migration; confirm no other file using `attached_window`/`attached_pane` was missed via a final grep.
- Full targeted-test verification across every touched file plus a broad regression run, matching Tier 1/2's own scope (not a full `pytest tests/`, per this session's standing preference for touched-file-scoped verification).

## Explicitly out of scope

- Phase 4 (delete dead code) — this is the last Phase 3 item; log anything found belonging to Phase 4 instead of doing it here.
- Item 25's actual test-porting work (the ~180 tests that would need to construct new collaborator classes directly) — explicitly bigger than this tier's scope, per the plan's own words.
- Any Tier 1/2 item — all done, don't re-touch.

## Quality bar, matching every prior target this session

Adversarial review against HEAD, not assumptions or this prompt's own freshness-check guesses (re-verify all of the above yourself, especially item 23's exact count, item 26's which-sub-claim-is-actually-stale, and item 28's full file list). `ruff check` clean on every touched file, **including test files this time** — Tier 2's own gap-check found that check had been skipped on test files and caught a real issue as a result. Findings doc (`design_docs/phase3_tier3_findings.md` or similar) — lead with which items were fixed vs. verified-already-fine vs. deliberately left as documentation-only (item 25, and item 27 if you choose to keep deferring it). No commits — leave everything in the working tree for review.
