# Prompt: Phase 3, Tier 2 — real but lower-risk bugs (remaining items)

Paste this to the implementing agent as-is.

---

Execute Phase 3, Tier 2 of `docs/AUTOPILOT_REFACTOR_PLAN.md`. Second item in Phase 3 — Tier 1 (8 items) is done; read `design_docs/phase3_tier1_findings.md` for the established rigor and format before starting, same conventions apply here (regression test first where practical, then fix — no commits, leave in the working tree for review).

## Read first

`docs/AUTOPILOT_REFACTOR_PLAN.md` §5, Tier 2, items 9-21 (13 items, plus the "Reconciled 2026-08-17" note between items 20 and 21 — read that too, it's load-bearing). **Two of the thirteen are already fixed, two more need re-verification before trusting the plan's own characterization** — re-check every item yourself before starting, this handoff's freshness check is current as of its own writing, not guaranteed current by the time you start.

## Freshness check — confirmed as of this handoff, re-verify

- **Item 9 (`cleanup_all_stale_branches` stale-prefix filter) — still live, exactly as described.** `src/core/worktree_manager.py:1138`: `b.startswith(("agent-", "autopilot-", "feature_architect/"))` — unchanged, still missing `feature/{design}` and `feature/{design}/{feature}` patterns, still matching a prefix (`autopilot-`) nothing produces.
- **Item 10 (`tickets_api.py` untimed `subprocess.run` + FTS5 injection audit) — still live.** `get_commit_diff_endpoint` (`src/mcp/tickets_api.py:1262`) has three `subprocess.run` calls at lines 1312, 1335, 1359, none with `timeout=`. `_fts5_query()` (`src/services/ticket_search_service.py:27`) exists and is used by `keyword_search` (confirming `75a35b3`'s historical fix is intact) — the remaining ask is the audit: grep for other agent-/user-supplied text reaching a query-language context (FTS5 `MATCH`, `WorkflowOrchestrator._check_condition`'s grammar) without the same tokenize-and-quote treatment. Not yet done as far as this handoff can tell.
- **Item 11 (`stop_workflow` vs `reset_phase`'s subprocess offloading) — still live; the plan's own "becomes moot once §4.2 unifies both routes' termination call" turned out wrong.** §4.2 (this session) landed and unified the *agent-termination* call (`terminate_agent()`), but never touched the *tmux kill-session* `subprocess.run` each route makes independently. Confirmed by direct read of `src/mcp/frontend/_shared.py`: `stop_workflow` (line 2108) still calls raw `subprocess.run(...)` inline; `reset_phase` (line 2185) still correctly wraps the identical operation in `await loop.run_in_executor(None, functools.partial(subprocess.run, ...))`. Fix `stop_workflow` to match `reset_phase`'s pattern.
- **Item 12 (TurboVecStore/VectorStoreManager dimension mismatch) — partially already handled, re-verify the remaining gap.** `TurboVecStore.store_memory` (`src/memory/turbovec_store.py:171`) already raises `ValueError` per-call on a dimension mismatch — not the "silently swallowing" the plan describes, at least not at this layer. What this handoff did **not** verify: whether any of `store_memory`'s five callers (`rag.py`, `memory_api.py`, `task_completion/memory.py`, `ticket_search_service.py` x2) catches and swallows that `ValueError`, and whether `VectorStoreManager` (the turbovec/Qdrant abstraction) or any startup path validates `EMBEDDING_BACKEND`'s output dimension against the configured store's collection dimension at all. Check both before deciding whether a startup-time assertion is still the right fix or whether the per-call guard already covers the real risk.
- **Item 13 (blocking calls in async handlers needing `run_in_executor`) — needs full re-enumeration, not just a path update.** The plan's own text already flags this ("re-enumerate against post-decomposition file locations before fixing") — `autopilot_api.py` and `src/mcp/api.py` no longer exist (same staleness trap as §4.11 and Tier 1's item 8); `tickets_api.py` and `agents_api.py` still exist at their original paths. Grep fresh for blocking calls in async route handlers, don't reuse the plan's original list.
- **Item 14 (`post_phase_prompt_preview`'s hardcoded `DatabaseManager`) — likely already fixed, verify before doing more.** Now `src/mcp/frontend/phase_routes.py:92`: `db_manager = DatabaseManager(None)`, not the literal string `"hephaestus.db"` the plan describes. `DatabaseManager.__init__` (`src/core/database.py:1340-1343`) already resolves `None` via `os.environ.get("HEPHAESTUS_TEST_DB", "hephaestus.db")` — the same env-var fallback every other correctly-wired endpoint gets. Since `DatabaseManager` caches engines by resolved path (`_engines: Dict[str, Any]`), a fresh instance constructed with the same resolved path as the injected `frontend_api.db_manager` shares its cached engine — likely already equivalent in practice, even though it's still a separate object rather than a literal reuse of the injected instance. Verify this reasoning holds (confirm both resolve to the identical path in a real test-env run) before deciding whether any further change is needed, or whether this item is done.
- **Item 15 (`validate_file_path`'s superficial traversal check) — still live, exactly as described.** `src/services/validation_helpers.py:7-21`: `if ".." in str(file_path): raise ValueError(...)` — still a literal substring check, never confirms the resolved path stays within an expected root. `cost_collection_service.py`'s `resolve()` + prefix-containment pattern (cited by the plan as the template) — re-locate and confirm it's still there before copying it.
- **Item 16 (`TicketService._check_circular_blocking` one-hop only) — still live, exactly as described.** `src/services/ticket_service.py:135-160`: only checks whether a directly-blocking ticket is itself directly blocked by the ticket being updated — a longer chain (A→B→C→A) isn't detected. Needs real graph traversal (DFS/BFS over `blocked_by_ticket_ids`), not a bigger pairwise check.
- **Item 17 (rejected/timed-out tickets hard-delete their own audit trail) — still live, exactly as described. This is a product decision, not an obvious bug fix — get explicit direction before implementing.** `reject_ticket` (`ticket_service.py:1713`) itself doesn't delete — its own comment says "(will be deleted by create_ticket flow)" — the actual hard-deletes are at `ticket_service.py:450` and `:520` (`db.delete(ticket)`), presumably in the coroutine that awaits `approval_manager`'s decision. Confirm both delete sites' exact trigger conditions, then present the retain-and-mark-rejected vs. keep-hard-delete choice explicitly rather than picking one silently.
- **Item 18 (`spawn_validator_agent`'s discarded diff/results values) — still live, exactly as described.** `src/validation/validator_agent.py`: `branch_manager.get_workspace_changes(...)` and `get_agent_results(...)` are both called, but neither's return value reaches `format_task_validation_prompt`'s parameter list (confirmed by direct read this handoff, same function touched by Phase 3 Tier 1's item 7 gap-check for an unrelated reason — the `working_directory=... or "/tmp"` fallback). Check git blame/commit history for whether the omission was intentional before assuming it's pure debris, per the plan's own caution.
- **Item 19 (terminal-vs-non-terminal status conflation, opportunistic streamline) — still applicable, no specific line to re-verify.** This is a pattern-level suggestion (replace an overloaded `None`/string return value with a typed result), not tied to one exact current bug. Re-read the two cited historical fixes (`a949043`, `8249365`) if useful context, but there's nothing here that's gone stale — treat as still-open opportunistic work.
- **Item 20 (`_retry_failed_tasks`'s orphan-retry-cap dead code) — ALREADY FIXED, not stale text.** `get_tasks()` (`src/autopilot/orchestrator/engine_client.py:731`) now includes `"failure_reason": t.failure_reason` in its returned dict (line 755) — the exact fix the plan calls for. The regression test the plan cites, `tests/test_orchestrator_helpers.py::TestRetryFailedTasks::test_orphaned_task_incorrectly_capped_bug`, passes as of this handoff. Nothing to do here.
- **Item 21 (`_auto_restart_agent`'s raw `get_session()`) — ALREADY FIXED, not stale text.** `src/monitoring/auto_restart.py` has zero raw `.get_session()` calls left — both write paths use `session_scope()` (lines 58, 95). Nothing to do here.

**Net scope after freshness check: items 9, 10, 11, 15, 16, 18, 19 are confirmed live work. Items 12 and 14 need a verification pass before you can tell whether there's remaining work (likely small or none). Item 13 needs a fresh audit, not a reuse of the plan's file list. Item 17 needs a product decision from the user before implementing — don't silently pick retain-vs-delete. Items 20 and 21 need no code changes.**

## Target

For each confirmed-live item, fix narrowly per the plan's own description (re-read the relevant paragraph above — most are already specific about the fix shape). In particular:

- **Item 9**: extend the branch-prefix filter to cover `feature/{design}` and `feature/{design}/{feature}`, and drop or confirm `autopilot-` (the plan says it matches nothing ever produced — verify that's still true before removing it, a prefix matching nothing is harmless but removing a genuinely-dead check isn't this item's job unless it's clearly safe). Reconcile with `worktree_manager.py`'s existing branch-naming conventions rather than inventing a new one.
- **Item 10**: add `timeout=` to all three `subprocess.run` calls in `get_commit_diff_endpoint`. Separately, do the FTS5/query-grammar injection audit described above and fix any found gap using `_fts5_query`'s tokenize-and-quote pattern.
- **Item 11**: change `stop_workflow`'s inline `subprocess.run(...)` to match `reset_phase`'s `run_in_executor` wrapping, in the same file.
- **Item 15**: adopt `cost_collection_service.py`'s `resolve()` + prefix-containment pattern in `validate_file_path`.
- **Item 16**: replace the one-hop check in `_check_circular_blocking` with real graph traversal (DFS/BFS) over `blocked_by_ticket_ids`.
- **Item 18**: wire `get_workspace_changes`'s and `get_agent_results`'s return values into `format_task_validation_prompt`'s call, unless git history shows the omission was deliberate (state which you found).
- **Item 19**: opportunistic — if you have capacity after the above, introduce a typed result (enum or small dataclass) at the specific overloaded-return-value site(s) the plan names; otherwise defer and say so in findings, this one is explicitly lower priority than the rest.

## Verification

- Per-item regression test where the fix has a clear behavioral before/after (items 9, 10's timeout addition is harder to test meaningfully — a smoke test that the call sites now pass `timeout=` is enough, don't over-engineer a subprocess-hang integration test; 11, 15, 16, 18).
- Full targeted-test verification plus a full-suite gate against the pristine-HEAD baseline (strict subset of pre-existing failures, zero regressions). Use `git stash push --keep-index -- <file>` isolation per prior items in this session if a failure's origin is ambiguous.
- Item 17: no code changes until the product decision is made — if the user gives direction mid-implementation, treat it as its own small fix once resolved, not folded silently into another item's commit.

## Explicitly out of scope

- Items 20, 21 — confirmed already fixed above, don't touch.
- Tier 1 (done) and Tier 3 (items 22-28, not yet started) — log anything found belonging to those instead of fixing it here.
- Phase 4 (dead code deletion).
- Picking a direction on item 17 without asking — this is the one item in this batch that's a product call, not an engineering one.

## Quality bar, matching every prior target this session

Adversarial review against HEAD, not assumptions or this prompt's own freshness-check guesses (re-verify all of the above yourself, including the two "already fixed" and two "needs verification" claims). `ruff check` clean on every touched file — verify pre-existing findings via `git show HEAD -- <file>`. Findings doc (`design_docs/phase3_tier2_findings.md` or similar) — lead with which items needed no work vs. which were actually fixed vs. which are blocked on the item-17 product decision, matching Tier 1's findings-doc structure. No commits — leave everything in the working tree for review.
