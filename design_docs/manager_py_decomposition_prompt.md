# Prompt: split `src/agents/manager.py`

Paste this to the implementing agent as-is.

---

Split `src/agents/manager.py`'s `AgentManager` class (currently ~3,430 lines, 48 methods) into a package, the last of the five Phase 1b god-objects — the other four (`src/mcp/api.py`, `MonitoringLoop`, `task_completion_service.py`, plus `create_agent_for_task`/`restart_agent`'s internal decompose+dedup) are done; this is the one that never got a file-level split. It's structurally closer to the `MonitoringLoop` extraction than to the route-file splits: one large stateful class, not free functions/routes, so the same collaborator-class-composed-as-an-instance-attribute pattern applies, not a router aggregator.

**Read first**: `design_docs/phase_1b_decomposition.md` §4.3 (`MonitoringLoop`) as your primary template — same problem shape. Also read `git show 1998a11` and `git log -p -- src/agents/messenger.py src/agents/prompt_builder.py` (the commit that extracted `AgentMessenger`/`AgentPromptBuilder` out of this *exact* class, per `docs/SOLID_OO_REVIEW.md` finding 3.1) — it's a live precedent already sitting in this codebase, composed in `AgentManager.__init__` as `self._messenger`/`self._prompt_builder`, with existing delegator methods (e.g. `send_message_to_agent`) kept on `AgentManager` for callers that still reach through it. Match that pattern exactly for whatever you extract.

**Do your own freshness check first** — don't trust any line numbers from earlier planning docs; this file changed materially during the `create_agent_for_task`/`restart_agent` work. Enumerate all 48 current methods with `ast.parse()`-verified line ranges and cluster them by responsibility. From what's known as of this handoff, expect roughly: the launch pipeline (`create_agent_for_task`, `restart_agent`, and the 11 shared step methods added during the last split — `_resolve_worktree`, `_resolve_env_and_model`, `_detect_launch_failure`, `_check_termination_race`, etc. — plus `_create_tmux_session`, `_export_env_vars_and_verify`, `_send_goal_command`, `_verify_instructions_file_read`, `_gather_worktree_context`, `_format_initial_message`, `_verify_prompt_delivery`, `_record_cli_session`, `_send_initial_prompt_with_retry`, `_wait_for_shell_ready`); termination (`terminate_agent`, `_commit_wip_in_shared_worktree`); transcript/output capture (`get_agent_output` and its ~10 helper methods); messaging (already extracted — confirm nothing new has accreted back onto `AgentManager` directly since). Verify this grouping against the live file rather than assuming it's still accurate.

**The real design decision, mirroring what the `FrontendAPI`-stays-one-class choice was for the `api.py` split**: does `AgentManager` stay one class composing new collaborators (matching the `messenger.py`/`prompt_builder.py` precedent exactly), or does some cluster warrant its own class hierarchy? Check for cross-cluster call edges the way that split's research did for `FrontendAPI` — if the launch pipeline, termination, and transcript-capture clusters only call into each other through `self.<collaborator>.<method>()` once extracted, one-collaborator-per-cluster is right; if there's heavy cross-calling, say so and propose accordingly rather than defaulting.

**External call sites — production and tests, both, exhaustively** (this codebase's two prior splits both learned the hard way that test call-site migration gets missed if it's not treated as first-class): `AgentManager` is instantiated and called from many places (`agents_api.py`, `feature_routes.py`, `monitor.py`'s several call sites, `engine_client.py`, `agent_dispatch_service.py`, `validator_agent.py`, `server.py`'s startup-resume path — see `design_docs/phase_1b_decomposition.md` §4.2's own call-site table for `create_agent_for_task`/`restart_agent` specifically, still accurate). `tests/test_agent_manager.py`, `tests/test_prompt_delivery_cleanup.py`, `tests/test_worktree_integration.py`, `tests/test_restart_agent_characterization.py`, and `tests/test_monitor.py`'s `TestHandleMissingTmux` all call methods on a live `AgentManager` instance directly by name — same delegator-stub requirement `MonitoringLoop`'s split had (loud `AttributeError` on a miss, not a silent string-patch failure, but still needs every one covered).

**Do the actual extraction with a script, not by hand.** Write `scripts/split_manager.py` (or similar) that parses `manager.py` via `ast`, extracts each cluster's methods by verified line range, and generates the new module files programmatically — the same scripted-extraction methodology `design_docs/backend_module_decomposition.md` §3.3 specifies and `scripts/split_autopilot_api.py` already proved twice on this codebase (the `autopilot_api.py` split, and again for `api.py` in Phase 1b). Hand-copying code between files is exactly what that methodology exists to avoid — it's how a stray line gets dropped silently (see the `Workflow` import that went missing when `health_audit.py` was extracted, found only by a later `ruff` pass). The script should assert its own extraction is lossless (byte-identical reassembly check: concatenating the generated files' moved content back together reproduces the original method bodies verbatim) before you trust its output. Leave the script in `scripts/` alongside the others when done, don't delete it.

**Quality bar, matching the other four targets**: adversarial review against HEAD, not assumptions; `ruff check` clean; full targeted-test verification per cluster plus a full-suite gate against the pristine-HEAD baseline (strict subset of pre-existing failures, zero regressions); anything found outside this scope logged in a findings doc, not fixed inline, per the zero-behavior-change rule. No commits — leave everything in the working tree for review.
