# Prompt: Phase 3, Tier 1 — live correctness bugs (remaining items)

Paste this to the implementing agent as-is.

---

Execute Phase 3, Tier 1 of `docs/AUTOPILOT_REFACTOR_PLAN.md`. First item in Phase 3 — Phase 2 (§4.1 through §4.11) is fully done; read `design_docs/phase2_*_findings.md` for the established rigor and format before starting, same conventions apply here (regression test first, then fix — no commits, leave in the working tree for review).

## Read first

`docs/AUTOPILOT_REFACTOR_PLAN.md` §5 ("Phase 3 — Fix confirmed live bugs"), Tier 1, items 1-8. **Four of the eight are already fixed** — re-verify before trusting the plan text, then work only the remainder.

## Freshness check — confirmed as of this handoff, re-verify

Read every item's cited file directly rather than trusting the plan's line numbers or status (Phase 2 work this session, and the concurrent test-fixing session's own changes, have moved or fixed several of these since the plan was written):

- **Item 1 (cross-workflow validator-outcome race, `memory_api.py`) — ALREADY FIXED, not stale text.** `submit_result_validation` (`src/mcp/memory_api.py:834`) resolves the validator agent via `.filter(Agent.agent_type == "result_validator", Task.workflow_id == result.workflow_id).order_by(Agent.created_at.desc()).first()` — scoped by `workflow_id`, with an inline comment: "Scoped by workflow_id to prevent cross-wiring outcomes from concurrent validation runs across different workflows." Nothing to do here.
- **Item 2 (`orphan_reaper.py` `datetime.now()`) — two-thirds fixed, one instance remains.** The plan cites three sites (`:111,125,147`, now shifted). Reading the file: the grace-window check for terminating an orphaned agent (around line 117) already uses `datetime.utcnow()`, with an inline comment explicitly citing the UTC-only invariant and explaining the fix ("utcnow, not now: last_activity is stamped with datetime.utcnow() at every write site..."). **One instance remains live**: `current_time = datetime.now()` (`src/monitoring/orphan_reaper.py:139`), used for tmux-session-vs-DB grace-period tracking (`self.last_check_time`) later in the same method. This is the exact bug class CLAUDE.md's own incident narrative documents. Fix this one line.
- **Item 3 (`task_similarity_service.py:71`) — still live, exactly as described.** `query.filter(Task.phase_id is None)` — Python `is None` on a SQLAlchemy class attribute (`Task.phase_id`) always evaluates the Python identity check at query-build time (a `Column` object is never `None`), compiling to a `WHERE` clause equivalent to a constant, not a real `IS NULL` test — phase-less duplicate-task detection silently matches nothing, ever. Fix: `.is_(None)`.
- **Item 4 (`queue_service.py` `priority_boosted`) — still live, but only 2 of the plan's cited 3 sites are the actual bug.** Re-read `src/services/queue_service.py:374-391` directly: line 374's `not new_task.priority_boosted` is a Python bool negation on an actual instance attribute (a real boolean value) — correct, not part of the bug. Lines **380 and 390** both write `not Task.priority_boosted` — `Task.priority_boosted` there is the *class* attribute (an `InstrumentedAttribute`), and Python's `not` on it is always `False` (a non-empty object is truthy), so `and_(not Task.priority_boosted, not new_task.priority_boosted)` always evaluates that first operand `False` — the whole `and_()` branch is dead, permanently excluded from the `or_()` regardless of either task's actual boost state. Confirm the plan's regression-test requirement still applies: **write a regression test proving current queue-ordering behavior for two non-boosted tasks before fixing** — the practical impact of these two dead branches (inside `QueueService._calculate_queue_position`, `queue_service.py:333`) isn't yet confirmed either way, per the plan's own caution.
- **Item 5 (missing `terminated_at`, `reset_phase`/`stop_workflow`) and item 6 (`monitor.py` `_detect_orphaned_idle_agent`, missing `current_task_id` clear) — already fixed, via §4.2 this session**, per the plan's own note ("Fixed as part of §4.2's primitive consolidation, not separately — listed here for completeness"). §4.2's findings doc confirms both migrated to the shared `terminate_agent()` primitive, which sets all three invariant fields together. Nothing to do here — don't re-verify by hand, §4.2's own `tests/test_termination_invariant_single_writer.py` (an AST sweep) already guards this structurally.
- **Item 7 (`get_agent_branch_path`'s silent main-repo fallback) — still live, exactly as described.** `src/core/worktree_manager.py:774-782`: when no `AgentBranch` record exists (or it has no `worktree_path`), the function returns `str(self._project_root)` — the main repo path — instead of `None`. A caller like `restart_agent` that doesn't specifically check for "main repo path returned" can silently relaunch an agent into the main project repository instead of failing loudly. Fix: return `None` on the no-record path; let the caller fail loudly, matching the "fail loudly, don't silently redirect" principle the plan says is "already applied elsewhere in `create_agent_for_task`" — verify that citation still holds (re-read `create_agent_for_task` yourself) and match its actual error-handling shape, don't invent a new convention.
- **Item 8 (`review_feature`'s hand-rolled completion check) — ALREADY FIXED, not stale text.** `review_feature`'s approve handler (now `src/mcp/autopilot/feature_routes.py:588`, not the plan's stale `autopilot_api.py:4375-4397`) calls `derive_workflow_status(db, wf.id, write_back=False)` directly, with an inline comment: "use derive_workflow_status instead of hand-rolling this check. The 'all tasks done ≠ all phases done' mistake has recurred independently at least four times in this codebase's history." Nothing to do here. (Since this fix already routes through `derive_workflow_status`, the plan's own sequencing note — "sequence this before §4.6's broader wiring pass" — is moot; §4.6 already shipped this session too.)

**Net scope after freshness check: items 2 (one line), 3 (one line), 4 (two dead branches + a characterization test), 7 (one function's fallback behavior). Items 1, 5, 6, 8 need no code changes — verify and move on, don't re-fix what's already fixed.**

## Target

For each of the four still-live items:

1. **`orphan_reaper.py:139`**: change `current_time = datetime.now()` to `datetime.utcnow()`. Check whether `self.last_check_time` (compared against `current_time` later in the method) is itself ever set from a different clock elsewhere in the class — if so, that write site needs the same fix, this isn't necessarily a one-line change once you trace the full comparison.
2. **`task_similarity_service.py:71`**: change `Task.phase_id is None` to `Task.phase_id.is_(None)`.
3. **`queue_service.py:380,390`**: fix both `not Task.priority_boosted` occurrences to correctly test `Task.priority_boosted`'s actual (falsy) state in a SQL-expression-safe way — e.g. `Task.priority_boosted.is_(False)` or `Task.priority_boosted == False` (match whatever idiom the rest of this file/codebase already uses for a boolean-column negation; check `Task.priority_boosted.desc()`'s neighboring code and other boolean-column filters elsewhere in `queue_service.py` or `engine_client.py` for the established pattern before picking one).
4. **`worktree_manager.py:774-782`**: change the no-record fallback to return `None`. Find every caller of `get_agent_branch_path` (not just `restart_agent`, which the plan names as the one it already knows about) and confirm each one either already handles a `None` return correctly or gets a matching fix — a caller written assuming this function never returns `None` is a second half of the same bug if one exists.

## Verification

- **Item 2**: a regression test in `tests/test_orphan_reaper.py` (or wherever its existing tests live) proving the grace-period comparison uses UTC consistently — e.g. simulate a clock/timezone difference (or freeze both to a UTC value and a local-offset value) and confirm the grace window computes correctly regardless of the host's local timezone. Match the existing test style for the already-fixed instance in the same file, if a test for that one exists — reuse its pattern.
- **Item 3**: a test asserting a phase-less duplicate-task check (`phase_id=None`) actually excludes a near-duplicate phase-less task correctly — i.e. that the `.filter(Task.phase_id.is_(None))` clause is a real SQL predicate, not a no-op. The most direct proof: two phase-less tasks with near-identical descriptions in the DB, confirm the similarity check actually finds the pre-existing one (which it silently never could before the fix).
- **Item 4**: per the plan's explicit instruction, write the regression test **before** changing the two lines — it should characterize the *current* (possibly-buggy, possibly-coincidentally-fine) queue-ordering behavior for two non-boosted tasks with different `queued_at`/`priority` values, run it against pre-fix code to see what it actually asserts today, then fix the two lines and confirm the test's assertions about correct tie-break ordering (boosted-first, then priority, then queued_at) hold post-fix — if the pre-fix behavior was already accidentally correct for some inputs, say so explicitly in findings rather than silently discarding the pre-fix characterization.
- **Item 7**: a regression test for `get_agent_branch_path` confirming it returns `None` (not the project root) when no `AgentBranch` record exists, plus a check on every caller found — either an existing test already covers the caller's `None`-handling correctly, or a new test is added at the caller level proving it fails loudly instead of silently operating on the main repo.
- Full targeted-test verification plus a full-suite gate against the pristine-HEAD baseline (strict subset of pre-existing failures, zero regressions). Use `git stash push --keep-index -- <file>` isolation per prior items in this session if a failure's origin is ambiguous — do not disturb files you don't own (the concurrent test-fixing session may still be active).

## Explicitly out of scope

- Items 1, 5, 6, 8 — confirmed already fixed above, don't touch.
- Tier 2 and Tier 3 of Phase 3 (items 9-28) — log anything found belonging to those instead of fixing it here.
- Phase 4 (dead code deletion).
- Any broader refactor of `queue_service.py`'s tie-break logic beyond the two dead-branch lines — item 4 is a bug fix, not a redesign.

## Quality bar, matching every prior target this session

Adversarial review against HEAD, not assumptions or this prompt's own freshness-check guesses (re-verify all of the above yourself, including the four "already fixed" claims). `ruff check` clean on every touched file — verify pre-existing findings via `git show HEAD~1 -- <file>`. Findings doc (`design_docs/phase3_tier1_findings.md` or similar) — lead with which items needed no work vs. which were actually fixed, matching this handoff's own freshness-check structure. No commits — leave everything in the working tree for review.
